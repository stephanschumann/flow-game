{
  "name": "flow-game",
  "version": "1.0.0",
  "private": true,
  "description": "Flow Game - Lernspiel, Firebase Hosting + Firestore",
  "scripts": {
    "test:emulator": "firebase emulators:exec --only firestore \"jest tests/game-rooms.security.rules.test.js tests/game-rooms.logic.test.js\"",
    "test:emulator:logic": "firebase emulators:exec --only firestore \"jest tests/game-rooms.logic.test.js\"",
    "test:emulator:rules": "firebase emulators:exec --only firestore \"jest tests/game-rooms.security.rules.test.js\"",
    "test:deploy-regression": "jest tests/deploy-regression.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase-tools": "^13.29.1",
    "jest": "^29.7.0"
  }
}
